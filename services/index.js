// Backend services which compute blockchain and accounting calculations
// This part is closed source and disabled for Demo

const ChildProcess = require("child_process");

// Example on how to communicate between processess
// process.on('message', function(message) {
//     // Process data
//     process.send({id: message.id, data: 'some result'});
// });
//
//

module.exports = {
  initialize: () => {
    // Disabled for Demo
  }
};
